# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ai-tutor?retryWrites=true&w=majority

# Groq Configuration (for LLM study plan generation - replaces OpenAI)
GROQ_API_KEY=gsk_your-groq-api-key-here

# Pinecone Vector Database Configuration
# IMPORTANT: Use 384 dimensions for MiniLM-L6-v2 embeddings
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_INDEX=ai-tutor-index
PINECONE_DIMENSION=384
PINECONE_SKIP_PING=0

# Flask Parser Service (Cloud-hosted)
# This service handles PDF parsing and local embedding generation
FLASK_PARSER_URL=https://tutor-flask-1.onrender.com

# Backend Server Configuration
PORT=5000

# Firebase Configuration (for authentication)
# Add your Firebase credentials here if using Firebase Auth
